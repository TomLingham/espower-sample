'use strict';

const assert = require('power-assert');

describe('version', () => {
  it('should pull the version from the microservices', () => {
    return Promise.resolve().then(() => {
      return assert(true === false);
    });
  })
});

