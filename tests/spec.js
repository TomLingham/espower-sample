'use strict';

const assert = require('power-assert');

describe('version', () => {
  it('should pull the version from the microservices', () => Promise.resolve()
    .then(() => assert(true === false)));
});

